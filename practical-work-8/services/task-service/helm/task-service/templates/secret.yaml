apiVersion: v1
kind: Secret
metadata:
  name: {{ .Chart.Name }}-secret
  labels:
    app: {{ .Chart.Name }}
type: Opaque
stringData:
  db.username: {{ .Values.postgresql.username | quote }}
  db.password: {{ .Values.postgresql.password | quote }}

